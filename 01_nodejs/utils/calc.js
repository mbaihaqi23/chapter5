// function addition(x, y) {
//     return x +y;
// }

const addition = (x, y) => {
    return x + y;
};

const substract = (x, y) => {
    return x - y;
};

const multiply = (x, y) => {
    return x * y;
}
module.exports = {addition, substract, multiply};